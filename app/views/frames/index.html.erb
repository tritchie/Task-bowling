<h1>Listing frames</h1>

<table>
  <tr>
    <th>Ball1</th>
    <th>Ball2</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @frames.each do |frame| %>
  <tr>
    <td><%= frame.ball1 %></td>
    <td><%= frame.ball2 %></td>
    <td><%= link_to 'Show', frame %></td>
    <td><%= link_to 'Edit', edit_frame_path(frame) %></td>
    <td><%= link_to 'Destroy', frame, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Frame', new_frame_path %>
